# ── Dependencias principales ──────────────────────────────────
tensorflow>=2.15
opencv-python>=4.9
numpy>=1.26
Pillow>=10.0
scikit-image>=0.22

# ── OCR pre-entrenado (opcional, instalar uno) ────────────────
# easyocr>=1.7      # OCR rápido con buen soporte para español
# paddleocr>=2.9    # OCR de alta precisión

# ── Decodificación PDF417 (opcional pero recomendado) ─────────
# pyzbar>=0.1.9     # Requiere ZBar instalado en el sistema
# pdf417decoder>=0.1

# ── Utilidades ────────────────────────────────────────────────
# tqdm>=4.66        # Barras de progreso para lotes
